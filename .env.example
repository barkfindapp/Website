# Resend (confirmation emails)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
RESEND_AUDIENCE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx  # legacy fallback only

# Supabase — unified early-access waitlist (public.early_access)
# Uses the publishable/anon key; the table's RLS is insert-only so the list stays private.
SUPABASE_URL=https://kwwnwniyijeuorpbyvmn.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxx   # Supabase dashboard → Settings → API
